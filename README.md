# parametric-01.iverfinne.no

A parametric 3D studio. The UI shell (viewer stage, controls panel,
gestures, shareable URL state) comes from parametric.iverfinne.no; the
generative motor is its own — see lib/engine.ts (parameter space) and
components/engine-mesh.tsx (geometry mount).
